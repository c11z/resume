{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */resume.md) v=$(grep -m1 '^version:' \"$f\" | sed 's/version: *//'); maj=${v%%.*}; min=${v##*.}; sed -i '' \"s/^version: .*/version: ${maj}.$((min + 1))/\" \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Bumping resume version..."
          }
        ]
      }
    ]
  }
}
